[package]
name = "hamoltonian-chaos"
version = "0.1.0"
edition = "2024"

[dependencies]
nalgebra = "0.32"# Linear algebra for state vectors
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"# For exporting data to Python
rand = "0.8"
rayon = "1.10.0"# For random perturbations